image: node:20

pipelines:
  default:
    - step:
        name: Run Sample Test Case
        caches:
          - node
        script:
          - yarn install
          - yarn test
            
            

