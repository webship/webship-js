image: node:20

pipelines:
  default:
    - step:
        name: Install and Test
        services:
          - selenium
        caches:
          - node
        script:
          - npm install
          - FORCE_COLOR=1 npm test

definitions:
  services:
    selenium:
      image: selenium/standalone-chrome:latest
