image: node:16
stages:
  - test

test:
    stage: test

    services:
    - selenium/standalone-chrome

    script:
    - yarn install
    - yarn run run -p 4444:4444 -p 5900:5900 selenium/standalone-chrome-debug --host=selenium__standalone-chrome
    - yarn test
