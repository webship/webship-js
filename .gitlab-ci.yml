image: node:16
stages:
  - test

test:
    stage: test

    services:
    - selenium/standalone-chrome

    script:
    - yarn install
    - yarn run http://selenium:4444/wd/hub --host=selenium__standalone-chrome
    - yarn test
